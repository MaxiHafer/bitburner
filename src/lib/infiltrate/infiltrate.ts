import {NS} from "@ns";
import {JSONLogger} from "/lib/logger/logger";

export async function main(ns: NS) {
    let doc = document;
    let log = new JSONLogger(ns, {debug: true, pretty: true});
    

    while(true) {
        doc.getElementsByClassName("css-qan7cn")
    }
}

function handleMuiRootChange() {

}